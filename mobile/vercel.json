{
  "version": 2,
  "buildCommand": "cd my-app && npm install && npx expo export --platform web --output-dir ../dist",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
